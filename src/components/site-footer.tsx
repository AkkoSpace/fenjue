export function SiteFooter() {
  return (
    <footer className="border-t border-border/70">
      <div className="mx-auto flex w-full max-w-[90rem] flex-col gap-2 px-5 py-8 text-xs leading-5 text-muted-foreground sm:flex-row sm:items-center sm:justify-between sm:px-8">
        <span>焚诀 · 以图为引，得其法。</span>
        <span>内容来自公开网络，仅作灵感整理。</span>
      </div>
    </footer>
  );
}

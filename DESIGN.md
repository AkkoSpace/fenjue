# 焚诀设计文档

## 设计概述

焚诀服务使用豆包、Gemini、ChatGPT、Grok 等工具生成图片的普通用户。核心闭环是：浏览图片，判断是否喜欢，复制完整提示词，前往自己的工具生成。

V0.1 只做纯文生图案例展示、分类标签发现、账户基础和统一作品发布，不做全文搜索、收藏、Reaction、授权审核或图片编辑。公开浏览、筛选、查看详情和复制提示词始终不要求登录；发布作品需要完成邮箱验证。

## 设计方向

- 现代中文编辑风：柔白、炭黑、中性灰，少量朱红强调。
- 古代元素只用于印记、章节和等级，不使用仿古纸张、火焰背景或暗黑游戏界面。
- 图片是视觉主体，提示词是核心操作对象。
- 「天地玄黄」规划为后续版本的用户 Reaction，不由编辑预设，也不进入 V0.1 内容模型。

## 架构设计

```text
Next.js Server Component
        |
        +-- getPromptPage()
        |     +-- Supabase 分页 + 分类标签聚合 -> 24 条公开作品
        |     +-- empty database       -> empty work list
        |     +-- missing/error        -> explicit failure
        |
        +-- 首页 PromptFilters -> 分类 + 标签 URL 组合筛选
        |            |
        |            +-- PromptEntry -> PromptGallery -> NSFW 默认模糊保护
        |                         +-- 首张高优先级 + 其余原生懒加载
        |
        +-- 详情页 PromptGallery + PromptCopyButton
                                      |
                                      +-- shadcn/ui Button

robots.txt + sitemap.xml
        |
        +-- 可抓取边界 + 全量公开详情发现

Vercel Web Analytics
        |
        +-- 根布局 Analytics -> 匿名页面访问与站内路由切换

Vercel Speed Insights
        |
        +-- 根布局 SpeedInsights -> 匿名 Core Web Vitals 与真实体验分数

Supabase Auth
        |
        +-- 邮箱 + 密码注册 / 登录
        +-- Resend SMTP -> 邮箱确认 / 密码重置
        +-- Proxy 刷新会话 -> 账户页 / 后续管理页
        +-- profiles + RLS -> user / admin + single super admin
        |
        +-- /submit -> 登录用户统一发布 -> R2 预签名直传 + Supabase 事务写入
        |     +-- 主分类 -> categories 外键
        |     +-- 标签 -> prompt_tags 多对多关系（1-6 个）
        |     +-- 内容关系 -> 原创 / 转载 / 改编
        |     +-- 已验证工具 -> 作品与工具多对多关系
```

## 核心组件

- `src/lib/content/queries.ts`: 从 Supabase 查询作品元数据，并将 R2 object key 转换为公开图片地址。
- `scripts/import-nano-banana-pro.mjs`: 将可信 CSV 流式映射为焚诀分类、标签和转载模型，图片转为 WebP 后幂等写入 R2 与 Supabase。
- `src/lib/content/taxonomy.ts`: 维护分类标签类型、标签分组与输入规范，不在客户端复制数据库词表。
- `src/components/prompt-filters.tsx`: 将分类和标签筛选写入 URL，支持单独选择、组合、分享与清除。
- `src/components/prompt-entry.tsx`: 首页瀑布流中的单条作品，只展示图片、标题、多图数量与详情页入口。
- `src/components/prompt-gallery.tsx`: 保持图片比例的响应式画廊；首页只下载一张真实封面并用 CSS 纸片表达多图层级，详情页展示完整集合。
- `src/components/prompt-carousel.tsx`: 详情多图自动轮播；控制收在图片舞台内，并在悬停、聚焦、触摸、页面隐藏或减少动态效果时暂停。
- `src/components/account-menu.tsx`: 服务端读取当前账户与角色，访客返回登录入口，避免在浏览器端暴露权限判断。
- `src/components/account-menu-client.tsx`: 登录后的账户菜单交互；使用焚诀印记头像、身份摘要、发布与管理快捷入口，并支持键盘导航。
- `src/components/sensitive-image-guard.tsx`: 为 NSFW 作品提供默认模糊与单次点击显示，不跨页面持久化解锁状态。
- `src/components/prompt-copy-button.tsx`: 浏览器端复制和成功/失败反馈。
- `src/lib/supabase/*`: Supabase 浏览器、服务端和 Proxy 会话客户端。
- `src/lib/auth/actions.ts`: 邮箱密码注册、登录、找回密码、更新密码和退出的 Server Actions。
- `src/app/(login|register|forgot-password|reset-password|account)`: 访客优先的完整认证与账户流程。
- `src/app/prompts/[slug]/page.tsx`: 作品详情、作者来源、完整提示词和复制操作。
- `src/app/submit/page.tsx`: 登录用户统一作品发布入口。
- `src/components/submission/prompt-editor.tsx`: 多图预览、排序、删除和发布状态。
- `src/components/submission/taxonomy-selector.tsx`: 分类单选和标签多选，限制最多 6 个并保留键盘与触控可用性。
- `src/app/api/uploads/presign/route.ts`: 校验登录用户并签发短期 R2 PUT 地址。
- `src/lib/content/actions.ts`: 校验作品元数据并调用 Supabase 事务函数。
- `src/lib/content/ai-tools.ts`: 维护前端可选择的工具键、名称与稳定展示顺序。

## 设计决策

| 日期 | 决策 | 理由 | 影响 |
|------|------|------|------|
| 2026-07-30 | Next.js 16.2.12 + App Router | 保留 SSR、ISR、Streaming、Cache Components 和后续动态能力 | 需要 Node.js Runtime 与 Vercel 部署 |
| 2026-08-06 | 升级 Next.js 16.3.0 稳定版 | 修复旧版 PostCSS 与 Sharp 传递依赖的高危安全公告 | 同步升级 `eslint-config-next`，生产依赖审计恢复为 0 |
| 2026-07-30 | Tailwind CSS + shadcn/ui + Lucide | 复用可访问的交互基础，同时完整定制焚诀视觉 | 不直接采用 shadcn 默认主题 |
| 2026-07-30 | Supabase 元数据 + R2 object key | 图片和内容职责分离，支持后续扩展 | Supabase 与 R2 是唯一内容源，不提供本地数据回退 |
| 2026-07-30 | 首页发现 + 独立详情页 | 首页只服务连续看图，避免任何元信息遮挡或拉长作品流 | 首页只显示图片、标题和多图数量；详情页承接作者、来源、提示词和复制操作 |
| 2026-07-30 | 克制瀑布流 + 多图堆叠封面 | 提高同屏图片密度，同时让多图作品一眼可辨 | 手机单列、常规桌面双列、超宽桌面三列；一张真实封面配合最多两层 CSS 纸片并标注总数 |
| 2026-07-30 | 访客优先的 Supabase Auth | 浏览不应被登录阻断，但管理与未来 Reaction 需要稳定身份 | 邮箱密码、邮箱验证、密码重置与 RBAC；SMTP 由 Supabase 统一接入，当前使用 Resend |
| 2026-07-30 | 天地玄黄延后为用户 Reaction | 品阶应来自真实用户反馈，不应由编辑预设 | V0.1 删除 rank 字段和等级 UI，后续版本单独设计投票与聚合模型 |
| 2026-08-03 | 缓存公开内容查询 | Cache Components 要求远端数据位于显式缓存边界，且内容更新不需要逐请求查询 | `getPrompts()` 使用分钟级后台重验证和 `prompts` 标签；后续管理操作通过标签主动失效 |
| 2026-08-06 | 登录用户统一作品上传 | 上传本身是通用能力，管理员只是额外拥有全站管理权限的用户 | 新增作品所有权与 RLS、R2 写入配置、上传页面和作品事务写入函数；匿名浏览不变 |
| 2026-08-06 | 管理后台先聚焦内容管理 | 第一阶段只需要维护公开作品，不提前引入用户管理、审核队列和统计系统 | `/admin` 提供搜索、状态筛选、上下架和永久删除；上传继续复用 `/submit` |
| 2026-08-07 | 首个已验证账户成为唯一超管 | 需要稳定的初始管理身份，同时为以后增加普通管理员保留层级 | `profiles.is_super_admin` 使用部分唯一索引；事务锁防止并发产生多个超管，普通管理员继续使用 `admin` 角色 |
| 2026-08-20 | 接入 Vercel Web Analytics | 上线早期需要用真实访问数据判断内容与渠道表现 | 根布局统一采集匿名页面访问；V0.1 不发送自定义事件或用户身份信息 |
| 2026-08-20 | 接入 Vercel Speed Insights | 图片站的加载速度、响应性与视觉稳定性直接影响浏览体验 | 根布局采集匿名 Core Web Vitals；不接入业务数据或用户身份 |
| 2026-08-20 | 增加作品级 NSFW 保护 | 敏感图片需要由访客主动选择查看，同时不阻断作品收录与提示词复用 | 上传时默认关闭；首页、详情和后台缩略图默认模糊，点击后仅在当前页面显示 |
| 2026-08-20 | 增加内容关系标记 | 用户需要区分作者原创、原样转载与二次改编，同时保持首页以图片为主 | 上传时明确选择；现有网络收集内容默认为转载，仅在详情和后台展示 |
| 2026-08-20 | 增加多选已验证工具 | 同一提示词可能在多个工具中实际验证，且“已验证”不能等同于推测兼容 | 使用工具目录与作品—工具多对多表；上传可留空或多选，详情和后台展示，首页保持纯图片浏览 |
| 2026-08-20 | 详情多图改为轮播 | 并列展示会压缩每张图片，尤其不利于不同画幅的完整观察 | 单图保持原样；多图使用完整适配舞台、方向按钮、缩略图、键盘与触控切换 |
| 2026-08-21 | 轮播升级为自动内嵌控制 | 分离在图片外的控制条更像图片查看器，无法形成完整、连续的轮播体验 | 5 秒自动切换；方向、暂停与分页圆点覆盖在舞台内，保留触控、键盘和减少动态效果支持 |
| 2026-08-21 | 顶部账户入口升级为身份菜单 | 登录后只显示“账户”无法确认当前身份，也缺少发布与管理的高频入口 | 服务端读取身份；访客保持登录链接，登录用户显示印记头像、身份摘要、发布入口与按角色展示的内容管理 |
| 2026-08-21 | 收敛页头并压缩页脚 | 上传已存在于账户菜单，GitHub 也不是核心浏览操作，重复展示会削弱图片内容层级 | 页头只保留章节与账户；GitHub 移至单行紧凑页脚，移动端省略较长来源说明 |
| 2026-08-21 | 增加受控分类与标签体系 | 作品数量增长后只靠连续浏览难以发现目标效果，同时自由文本标签会快速产生同义词和脏数据 | 每条作品选择一个主分类和 1-6 个受控标签；首页通过阻塞式 SSR 提供 URL 组合筛选，详情与后台展示，首页卡片仍保持图片优先 |
| 2026-08-21 | 建立大规模可信数据导入管道 | 15,000 级内容与 24,000 级图片不能依赖手工录入，也不能让 CSV 成为线上数据回退 | 外部 ID 作为幂等键；图片转 WebP 写入 R2；首页数据库分页；缺图和异常来源只入后台不公开 |
| 2026-08-21 | 建立性能与技术 SEO 基线 | 24 张封面同时 preload、筛选链接批量预取和缺失 sitemap 会同时损害 LCP、带宽与内容发现 | 仅首图高优先级，其余懒加载；禁用卡片/筛选预取；分页自 canonical；单筛选可索引、组合筛选 noindex；全量公开详情进入 sitemap |

## 已知限制

- V0.1 已包含用户登录与账户基础，但没有 Reaction 或投票；天地玄黄仅保留在后续 Roadmap。
- 暂不提供图片压缩、裁剪或格式转换服务。
- 第一版作品发布后直接公开；预签名上传中途取消可能留下 R2 孤儿对象，后续通过生命周期清理、限流和审核流程处理。
- 分类与标签当前通过数据库迁移维护，尚无管理员词表编辑界面；新增或停用词条需要代码审查和迁移，避免词表失控。
- 批量导入的分类和标签来自确定性关键词规则，只保证符合受控词表与数量约束，不等同于人工编辑判断；待复核内容需要后台逐步校正。
- 本轮 CSV 有少量记录没有源图片或字段异常，这些记录会保留在 Supabase 并保持下架；当前后台还不能直接为既有记录补图，需要后续增加编辑能力。
- Supabase 与 R2 无法组成同一事务；永久删除先移除数据库记录再清理图片，R2 清理失败时需要按后台提示人工检查孤儿对象。
- Resend 发件域名、DNS 与 Supabase SMTP 仍需在各自控制台完成配置；仓库只保存通用参数和邮件模板，不保存凭据。

## 安全考量

- 公开内容页只读取 `published = true` 的数据，也不读取用户会话；认证故障不会阻断访客浏览与复制。
- Supabase Publishable Key 可以进入浏览器，权限由 RLS 约束；Service Role Key、Resend API Key 和 R2 写入凭据不得进入客户端、源码或 Git。
- 密码由 Supabase Auth 托管，应用数据库不保存密码或密码哈希；应用仅校验 10-128 字符并通过 TLS 调用 Supabase。
- 注册失败在原页面返回结构化错误，输入只保留在当前标签页的临时 React 状态中；服务端不回传密码，也不把密码或注册错误写入 URL、日志和持久化存储。
- 找回密码始终返回相同结果，不暴露邮箱是否注册；登录失败默认使用统一提示。
- 重复邮箱注册沿用 Supabase 的模糊响应，不根据账户是否存在显示不同结果；统一提示同时说明新用户查收邮件、已有用户登录或找回密码。
- 重新发送确认邮件始终返回相同结果，不暴露邮箱是否存在；发送动作只使用邮箱字段，不要求再次输入密码。
- 注册确认、密码重置和登录后的 `next` 仅允许站内绝对路径，阻止开放重定向。
- 邮箱确认和密码重置使用一次性 `token_hash` 由服务端 `verifyOtp`，不依赖注册或找回密码时的浏览器 PKCE Cookie；回调仍兼容 OAuth/PKCE `code`。
- 邮件模板使用服务端传入并受 Supabase Redirect URLs 白名单约束的 `RedirectTo`，不使用用户输入拼接外部域名；按钮和备用裸链接复用同一个一次性令牌。
- 普通新用户默认获得 `user` 角色；仅当系统尚无超管时，第一个完成邮箱验证的账户会在数据库事务锁内升级为 `admin` 并标记为唯一超管。
- `profiles.role` 与 `profiles.is_super_admin` 都不允许普通用户更新；部分唯一索引保证最多一个超管，内容写入策略仍调用 `is_admin()` 二次校验。
- 管理后台页面和每个变更 Action 都重新读取 Supabase 用户及 `profiles.role`；按钮可见性不作为权限边界，数据库 RLS 继续限制全站读取、更新和删除。
- 分类键和标签键在客户端、Server Action 与数据库事务函数中逐层校验；分类使用外键且不可为空，标签限制 1-6 个、去重并只能选择启用词条。标签读取 RLS 按匿名和已登录角色拆分，避免策略重叠。
- 后台搜索只接受有限长度的文字、数字与常用标识字符；作品 ID 必须通过 UUID 格式校验，操作完成后的返回地址只允许 `/admin`。
- 预签名地址只向已验证会话签发，且对象 key 必须使用当前用户前缀；R2 写入凭据仅在服务端使用。
- NSFW 只是一层默认展示保护，不是访问控制：R2 图片地址仍为公开资源，不能用于承载违法内容或替代内容审核。
- 外部作者和来源链接统一 `target="_blank"` + `rel="noreferrer"`。
- 提示词只作为文本渲染，不使用 `dangerouslySetInnerHTML`。
- JSON-LD 仅序列化服务端已读取的公开作品字段，通过 `JSON.stringify` 生成并将 `<` 转义为 Unicode 序列，不接受或执行任意 HTML/脚本。
- 已验证工具的键同时由服务端白名单和数据库外键校验；作品—工具关系沿用作品所有者与管理员 RLS，不信任客户端提交的任意字符串。
- Web Analytics 仅启用默认匿名页面访问，不发送邮箱、用户 ID、提示词内容或其他自定义事件；认证回调由 Route Handler 直接重定向，不加载浏览器统计组件。
- Speed Insights 仅发送匿名性能指标，不附加邮箱、用户 ID、提示词内容或自定义业务属性。
- 批量导入不使用 Service Role Key：一次性 `SECURITY DEFINER` RPC 仅接受随机 256 位密钥，锁定唯一超管、数据集标识、批次大小和受控外键；密钥只写入被 Git 忽略的本地文件，完成后立即撤销并删除 RPC。
- 外部图片请求只来自 CSV 中的 HTTP(S) 地址，响应限制 35MB、60 秒并经 Sharp 解码；源 URL 不进入 shell 或 SQL 字符串，数据库写入通过结构化 JSON 参数完成。

### 威胁模型与边界

- 防护对象：账户会话、管理员角色、未发布内容、SMTP/R2/Supabase 高权限凭据。
- 信任边界：浏览器只持有 Publishable Key 与 HttpOnly 会话 Cookie；Supabase Auth 校验身份，PostgreSQL RLS 校验数据权限，R2 API 凭据只存在于服务端，Resend 只负责邮件投递。
- 已知风险：新部署若在站点所有者确认首个账户前开放公开注册，陌生用户可能先成为初始化超管；上线流程必须先完成所有者邮箱验证。邮件可达率与域名声誉依赖 Resend/DNS 配置；Supabase 与 R2 的跨服务删除不具备原子性，上线前必须验证邮件链路和真实作品删除流程。

## 变更历史

### 2026-08-21 - 优化瀑布流性能与技术 SEO

**变更内容**：首页从 24 张封面同时 preload 调整为一张高优先级与其余原生懒加载；多图叠层改用 CSS 纸片，关闭卡片和筛选链接预取，移除列表查询中的重复精确计数，并为图片优化结果设置一年缓存。新增动态首页 metadata、canonical、筛选索引规则、全量 sitemap、robots、公开详情 Open Graph/Twitter 图片、面包屑与 `CreativeWork`/`ItemList` JSON-LD。

**变更理由**：15,000 级作品流需要控制首屏网络竞争与爬虫路径；无限滚动不利于稳定抓取，所有封面抢占优先级也会拖慢真正的 LCP 图片。

**影响范围**：首页瀑布流、筛选与分页链接、图片画廊、公开查询、根 metadata、公开详情、robots、sitemap、认证与管理工具页索引状态，以及项目文档；作品数据、上传与权限模型不变。

**决策依据**：保留服务端数字分页和可跟随链接；首页不预加载无法在不同视口唯一确定的 LCP，只提升第一张封面并让浏览器正常处理视口内懒加载；单分类/单标签提供独立标题并可索引，组合筛选保留使用价值但 `noindex`，避免生成大量重复薄页。

### 2026-08-21 - 建立 Nano Banana Pro 全量导入管道

**变更内容**：新增外部导入标识、来源描述和导入状态字段，扩展 8 个高频受控标签；提供 CSV 流式读取、图片下载校验、WebP 转换、R2 幂等 object key、Supabase 批量 upsert 与失败重试；首页改为每页 24 条数据库分页，详情页改为按 slug 单条读取。

**变更理由**：将 15,062 条提示词与 24,109 张来源图片纳入真实内容源时，必须避免一次加载全站数据、重复导入、热链失效和单条错误中断整个任务。

**影响范围**：Supabase 作品模型、标签目录与公开统计函数，R2 内容规模，首页和详情查询，后台异常标记，导入脚本、项目依赖与运维文档。

**决策依据**：外部 ID 与数据集名称组成幂等键，R2 key 使用外部 ID 和图片顺序以支持断点续传；只有图片和字段都完整的记录自动公开，缺图或失败记录进入后台待处理；一次性 RPC 使用高强度密钥并在导入结束后删除。

### 2026-08-21 - 增加分类与标签发现体系

**变更内容**：新增 6 个受控主分类和 23 个风格、形式、主题标签；首页支持分类与标签联动筛选，上传必须选择一个分类和 1-6 个标签，详情与后台同步展示。

**变更理由**：作品增加后只靠连续瀑布流难以快速找到目标效果，同时自由文本标签容易产生同义词、错别字和无效词条。

**影响范围**：Supabase 分类、标签及作品关联表，作品发布事务函数，公开与后台查询，首页 SSR、上传表单、详情页和项目文档。

**决策依据**：主分类保持单选以稳定信息架构，标签使用受控多选支持交叉发现；客户端、Server Action、数据库函数、外键与 RLS 共同约束数据有效性。

### 2026-08-21 - 收敛页头与页脚导航

**变更内容**：移除页头独立的上传和 GitHub 入口；上传继续由账户菜单承接，GitHub 移入页脚，同时将页脚压缩为约 56px 的单行信息栏。

**变更理由**：上传入口重复、GitHub 不属于核心浏览任务，页脚原有纵向留白也超过其信息量所需。

**影响范围**：全站页头、页脚和设计文档；上传路径、账户菜单、GitHub 地址及权限逻辑不变。

**决策依据**：桌面端展示完整来源说明，移动端保留品牌短句与 GitHub；交互链接仍满足 44px 触控目标。

### 2026-08-21 - 增加焚诀账户菜单

**变更内容**：登录后的顶部账户入口升级为印记头像与昵称，展开后展示身份、邮箱、我的账户、发布作品、按角色显示的内容管理和独立退出操作；访客仍显示简洁登录入口。

**变更理由**：让用户在不离开当前页面的情况下确认登录身份并进入高频操作，同时保持首页以图片浏览为核心。

**影响范围**：全站页头、账户会话读取与设计文档；公开浏览、认证权限和数据模型不变。

**决策依据**：账户与角色由 Server Component 读取，菜单交互使用 Base UI 的可访问语义；视觉采用方形印记、细线分区和少量朱红，不复制通用 SaaS 大圆角灰色菜单。

### 2026-08-21 - 轮播升级为自动内嵌控制

**变更内容**：详情多图改为 5 秒自动轮播，左右方向、暂停/继续和分页圆点全部收进图片舞台，并使用水平滑动过渡。

**变更理由**：原有图片外控制条和缩略图更接近手动查看器，视觉关系松散，也不符合用户对标准轮播的预期。

**影响范围**：详情轮播组件与设计上下文；首页多图封面、单图详情、内容数据均不变。

**决策依据**：鼠标悬停、键盘聚焦、触摸操作和页面隐藏时自动暂停；系统启用减少动态效果时关闭自动播放和滑动动画，并保留方向键、按钮与圆点跳转。

### 2026-08-20 - 增加多选已验证工具

**变更内容**：上传页允许多选 Nano Banana、豆包、Grok 和 ChatGPT；详情页与管理后台展示实际验证结果，并为已导入的 9 条 Nano Banana 案例完成回填。

**变更理由**：同一提示词可能在多个生成工具上有效，需要表达可叠加的真实验证记录，而不是互斥的单一平台或没有依据的“通用支持”。

**影响范围**：Supabase 工具目录与作品—工具关系、上传事务函数、公开及后台查询、上传表单、详情页和文档；首页作品流保持不变。

**决策依据**：采用规范化多对多模型便于后续增加工具、筛选和统计；未验证保持空集合，工具键经过前后端白名单、外键与 RLS 三层约束。

### 2026-08-20 - 详情页多图轮播

**变更内容**：多图详情由并列网格改为单图轮播，增加前后切换、计数、缩略图定位、方向键和触控滑动。

**变更理由**：让用户一次专注一张生成结果，并避免多图并排导致尺寸过小或画面被裁切。

**影响范围**：详情图片展示组件与设计上下文；首页多图堆叠封面保持不变。

**决策依据**：轮播控制放在图片舞台之外，使用 `object-contain` 完整呈现不同画幅，避免控件遮挡图片主体。

### 2026-08-20 - 增加原创、转载与改编标记

**变更内容**：作品增加内容关系字段，上传时可选择原创、转载或改编；详情页与管理后台显示结果。

**变更理由**：保留原作者和原始链接之外，还需要明确内容是否经过重新整理或修改。

**影响范围**：Supabase 作品模型与事务函数、上传表单、公开查询、详情页和管理后台。

**决策依据**：现有网络收集内容统一默认为转载；首页不展示该字段，继续优先服务图片浏览。

### 2026-08-20 - 增加 NSFW 图片保护

**变更内容**：上传表单增加作品级 NSFW 开关；被标记作品在首页、详情页和管理后台默认模糊，访客点击后才显示图片。

**变更理由**：允许收录具有参考价值的敏感题材，同时避免访客在连续浏览时被动看到不适内容。

**影响范围**：Supabase 作品模型与事务函数、上传表单、公开作品查询、首页、详情页和后台缩略图。

**决策依据**：开关默认关闭，解锁状态只保留在当前组件生命周期中；该能力只负责展示保护，不将公开 R2 地址误作权限边界。

### 2026-08-20 - 优化重复邮箱注册反馈

**变更内容**：注册完成提示同时覆盖新邮箱与已有账户，并在登录页明确重新发送验证邮件的使用方式。

**变更理由**：Supabase 为防止邮箱枚举会模糊重复注册结果，原来的单一“邮件已发送”提示可能让已有用户误以为创建了第二个账户。

**影响范围**：注册成功提示、登录页辅助操作和认证文档；账户唯一性、密码与邮件发送逻辑不变。

**决策依据**：所有邮箱继续得到相同响应，不泄露账户是否存在；通过清晰的下一步说明改善正常用户体验。

### 2026-08-20 - 接入真实用户性能分析

**变更内容**：在 Next.js 根布局接入 Vercel Speed Insights，收集真实用户的 Core Web Vitals 与体验分数。

**变更理由**：焚诀以图片连续浏览为核心，需要用真实设备和网络环境中的数据识别加载速度、交互响应与布局稳定性问题。

**影响范围**：全站根布局、生产依赖与部署；不改变页面功能、业务数据、认证、Supabase 或 R2。

**决策依据**：使用 Vercel 官方 Next.js 组件并保持默认采样；当前访问量较低，完整采样更有利于尽早获得有效基线。

### 2026-08-20 - 接入匿名访问分析

**变更内容**：在 Next.js 根布局接入 Vercel Web Analytics，覆盖页面加载与站内路由切换。

**变更理由**：上线初期需要了解真实访客、页面表现与来源渠道，为后续内容运营和产品迭代提供依据。

**影响范围**：全站根布局、生产依赖与部署；不改变业务数据、认证、Supabase 或 R2。

**决策依据**：V0.1 只使用默认匿名聚合访问统计，不启用包含用户身份或内容信息的自定义事件。

### 2026-08-20 - 修正生产邮件回调并增加备用链接

**变更内容**：注册确认与密码重置模板改为使用请求传入的 `RedirectTo`，并在按钮下增加可复制的完整验证地址。

**变更理由**：避免托管 Supabase 的旧 `Site URL` 让生产邮件回到本地地址，同时兼容邮件客户端按钮不可点击的情况。

**影响范围**：Supabase 邮件模板、认证部署文档与上线验收；认证回调和一次性令牌验证逻辑不变。

**决策依据**：`RedirectTo` 由服务端根据 `NEXT_PUBLIC_SITE_URL` 生成并由 Supabase 白名单校验；备用地址与按钮使用同一个短期一次性令牌，不扩大认证权限。

### 2026-08-07 - 增加唯一超级管理员

**变更内容**：为账户资料增加 `is_super_admin`，将全新数据库中首个已验证账户设为超级管理员，并在账户页显示对应身份。

**变更理由**：需要一个明确且唯一的最高管理身份，同时允许后续增加普通管理员而不扩散最高权限。

**影响范围**：Supabase profile 数据模型、注册确认触发器、账户身份展示、管理员授权与安全文档；现有 `/admin` 内容权限保持不变。

**决策依据**：数据库 advisory lock 处理初始化并发，部分唯一索引作为最终单例约束；不提供公开提升权限入口，避免客户端越权。

### 2026-08-06 - 增加管理员内容管理后台

**变更内容**：新增 `/admin` 作品列表、搜索、状态筛选、分页、上下架和永久删除，并在管理员账户页加入入口。

**变更理由**：统一上传能力已经建立，下一步需要用最小后台维护全站内容质量，但暂不扩大到用户管理、审核队列或统计系统。

**影响范围**：管理员鉴权数据访问层、Supabase 内容查询与变更 Action、R2 对象删除、账户页入口和管理文档；公开浏览与普通用户上传流程不变。

**决策依据**：页面和每个 Action 都执行服务端角色校验，RLS 作为最终数据边界；永久删除先移除数据库记录再清理 R2，优先避免公开内容破图，并对非原子清理失败给出明确提示。

### 2026-08-06 - 增加重新发送确认邮件

**变更内容**：登录页新增重新发送注册确认邮件的入口，服务端调用 Supabase `auth.resend` 并复用 TokenHash 邮件模板。

**变更理由**：用户可能点击过期邮件、重复注册未确认账户，或因邮件延迟需要再次发送；不应要求删除账户才能恢复流程。

**影响范围**：登录页面、认证 Action 和认证文档；不改变账户枚举防护、会话边界或数据库结构。

**决策依据**：无论邮箱状态和发送结果都返回统一提示，避免泄露注册状态；发送动作不记录或回传密码。

### 2026-08-03 - 修复跨浏览器邮件验证

**变更内容**：确认注册和重置密码邮件改用一次性 `token_hash`，认证回调通过 Supabase `verifyOtp` 建立会话，并保留原有 `code` 交换作为兼容路径。

**变更理由**：避免用户在其他浏览器、隐私窗口或设备打开邮件时，因缺少注册会话中的 PKCE 验证器 Cookie 而得到“验证链接无效”。

**影响范围**：认证回调、确认注册模板、密码重置模板和认证配置文档；不改变账户权限与数据库结构。

**决策依据**：令牌由 Supabase 生成、一次性使用并受过期时间约束，服务端验证后写入 HttpOnly 会话 Cookie；`next` 继续限制为站内路径。

### 2026-08-03 - 保留注册失败输入并细化密码反馈

**变更内容**：注册失败改为 Server Action 原地返回结构化错误，表单输入保留在当前标签页内存中，并将 Supabase 的密码长度、字符组合和泄露记录原因转换为具体中文提示。

**变更理由**：避免一次校验失败清空整张表单，同时让用户明确知道密码未通过的原因。

**影响范围**：注册页面、注册 Action、密码输入组件和认证错误展示；不涉及数据库结构或认证权限边界。

**决策依据**：服务端不回传密码，客户端仅在当前页面生命周期内保存输入，不使用 URL、日志、Cookie 或 Web Storage 持久化密码。

### 2026-07-30 - 引入访客优先账户系统

**变更内容**：加入邮箱密码注册、邮箱确认、登录、找回/重置密码、账户页、会话刷新、`profiles` 与管理员 RBAC，并为 Resend 提供通用 SMTP 配置和邮件模板。

**变更理由**：公开浏览仍保持零门槛，同时为后续 Reaction、收藏和站内内容管理建立可信用户身份。

**影响范围**：认证路由、站点页头、Supabase 客户端与 Proxy、数据库迁移、环境变量、邮件模板和部署配置。
